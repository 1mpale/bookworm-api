cache:
  backend: redis
  url: ${REDIS_URL:-redis://localhost:6379/0}
  default_ttl: 3600
  key_prefix: "bookworm:"
  pools:
    default:
      max_connections: 20
      timeout: 5
    sessions:
      max_connections: 10
      timeout: 3
      db: 1
  serializer: json
