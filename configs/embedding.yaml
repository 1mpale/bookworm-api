embedding:
  provider: openai
  api_url: ${EMBEDDING_API_URL:-http://localhost:8001}
  api_key: ${EMBEDDING_API_KEY}
  model: text-embedding-3-small
  dimensions: 1536
  batch_size: 100
  timeout: 30
  retry:
    max_attempts: 3
    backoff_factor: 2
